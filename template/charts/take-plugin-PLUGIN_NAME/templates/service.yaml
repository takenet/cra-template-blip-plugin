apiVersion: v1
kind: Service
metadata:
  name: {{ template "take-plugin-PLUGIN_NAME.fullname" . }}-{{ .Values.environment.stage }}
  labels:
    app: {{ template "take-plugin-PLUGIN_NAME.name" . }}
    chart: {{ template "take-plugin-PLUGIN_NAME.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "take-plugin-PLUGIN_NAME.name" . }}
    release: {{ .Release.Name }}